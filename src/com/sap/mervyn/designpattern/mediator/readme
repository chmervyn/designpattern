Mediator pattern(中介者模式)
用一个中介对象封装一系列的对象交互，中介者使各对象不需要显示的相互作用，从而使其耦合松散，而且可以独立的改变他们之间的交互。

优点：
减少类之间的依赖，把原有的一对多的依赖变成了一对一的依赖。同事类只依赖中介者，减少了依赖，也降低了类之间的耦合

缺点：
中介者会不停膨胀，而且逻辑复杂。同事类越多，中介者的逻辑就越复杂。

使用场景：
适用于多个对象之间紧密耦合的情况，紧密耦合的标准是：在类图中出现了蜘蛛网状结构。即各个对象相互依赖，关系复杂。