Observer Pattern(观察者模式): 定义对象间一种一对多的依赖关系，使得每当一个对象改变状态，则所有依赖于它的对象都会得到通知并被自动更新。

优点：
    观察者和被观察者之间是抽象耦合
    建立一套触发机制

缺点：
    开发效率和运行效率可以会有问题。

注意事项：
    广播链问题：一个观察者也可以是被观察者，所以一个观察者模式中最多出现一个对象即是观察者也是被观察者，即消息最多转发一次（传递两次）。
    异步处理问题：被观察者发生动作，观察者作出回应，如果观察者比较多，使用异步，就需要考虑线程安全和队列的问题。